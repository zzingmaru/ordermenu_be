<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 환경 설정 (예: 로그 설정) -->
    <settings>
        <setting name="logImpl" value="STDOUT_LOGGING" />
    </settings>

    <!-- Type Alias 설정 -->
    <typeAliases>
        <package name="com.gtit.api.vo" />
    </typeAliases>
</configuration>
